# Next.js 환경변수 예시
# 복사 후 실제 값을 채워서 사용:  cp .env.example .env.local
# .env.local 파일은 .gitignore에 의해 커밋되지 않습니다.

# API 서버 내부 URL (Next.js API Route → Spring Boot API 프록시)
INTERNAL_API_URL=http://localhost:8081
